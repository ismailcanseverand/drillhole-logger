import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kalemaden.drillholelogger',
  appName: 'KaleMaden Drillhole Manager',
  webDir: 'dist'
};

export default config;
